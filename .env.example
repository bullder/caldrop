# Neon (managed Postgres) connection string. Set in the Vercel project via the
# Neon integration. When unset, upload/amend persistence is silently skipped.
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require
